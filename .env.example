# Optional: only for background vision teacher on a server/Vercel deployment.
# Never expose this variable in browser JavaScript.
OPENAI_API_KEY=

# Recommended on public deployments. Enter the same token in Diagnostics.
VISION_ACCESS_TOKEN=
